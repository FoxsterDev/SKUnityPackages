using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SKSample : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("this is version 0.0.2");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}